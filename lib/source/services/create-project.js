'use strict';

var BASE_DIR = ('../../../lib/');
var logger = require(BASE_DIR + 'commons/logger');

// var model = require('../models');

function create(paylodaCreate, callback) {
  callback();
}

module.exports = {
  create: create
};
